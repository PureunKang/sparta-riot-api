import React from "react"
import { Item } from "@/types/Item"
import { fetchItemList } from "@/utils/serverApi"

const page = () => {
  // fetchItemList().then((data) => console.log("아이템", data))

  return <div>아이템 목록</div>
}

export default page
